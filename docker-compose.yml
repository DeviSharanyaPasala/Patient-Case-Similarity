version: '3.8'

services:
  api:
    build: .
    container_name: patient-similarity-api
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_APP=app.py
    volumes:
      - .:/app
    restart: unless-stopped
